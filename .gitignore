# Rust build artifacts
/target/
**/*.rs.bk
*.pdb

# Cargo.lock — see note below
# Cargo.lock

# Editor / OS junk
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Your tool's own output — never commit scan results
result.txt
result.json
findings.txt
findings.json
*.env
.env
.env.*

# Test fixtures that contain real secrets — keep a template instead
# tests/fixtures/real_creds.env

# Local overrides
.envrc